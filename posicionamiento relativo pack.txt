en vez de especificar coordenadas, solo le decimos que debe ir arriba, abajo,
izquierda o a la derecha respecto a algún contenedor.
Si no se indica argumento, por defecto TK posicionara los elementos uno arriba de otro

self.entry = ttk.entry(self)
entry.pack()

self.entry = ttk.entry(self,text="Hola, mundo!")
entry.pack()

-----------------
self.entry =ttk.entry(self)
self.entry.pack(side=tk.LEFT)

con after y before se puede controlar el orden de ubicación de los objetos

self.entry =ttk.entry(self)
self.entry.pack()

self.button = ttk.button(self,text="Hola, mundo!")
self.button.pack()

self.label = ttk.text(self, text="...desde Tkinter")
entry.label.pack(before=self.entry)

en orden lo que debería de aparecer sería 
mensaje "desde tkinter"
casilla
mensaje hola mundo

--------------------
padx y pady hacen referencia a los margenes externos, mientras que ipadx y ipady son internos


expand true/false
es para que se expanda según el cambio de tamaño de la pantalla

fill tk.BOTH, tk.X (horizontal, tk.Y (vertical))